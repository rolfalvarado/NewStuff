# Reglas del Proyecto / Project Rules

- **No tocar ni subir/descargar `robust-deploy.ps1`**: Al realizar operaciones con GitHub (push, pull, commit, checkout, etc.), no se debe modificar, sobreescribir ni subir el archivo `robust-deploy.ps1`. Debe mantenerse tal como está a nivel local para preservar la configuración del usuario.
